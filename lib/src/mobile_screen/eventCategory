import 'package:flutter/material.dart';

class EventCategoryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Phân Loại Sự Kiện")),
      body: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Column(
          children: [
            TextField(
              decoration: InputDecoration(labelText: 'Loại sự kiện (hội thảo, workshop, v.v.)'),
            ),
            ElevatedButton(
              onPressed: () {
                // Save event category
              },
              child: Text('Lưu loại sự kiện'),
            ),
          ],
        ),
      ),
    );
  }
}
